# Browser requests go to the current origin; Vite proxies /api in development.
VITE_API_URL=/
API_PROXY_TARGET=https://maqas.ru

# To use a locally running backend instead:
# API_PROXY_TARGET=http://127.0.0.1:8000
