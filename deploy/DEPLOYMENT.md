# Установка HTTPS — 14 сентября 2026

Основной адрес: https://maqas.ru. HTTP и https://www.maqas.ru перенаправляются на него с сохранением пути и параметров.

- Сервер: `root@72.56.250.175`.
- Рабочая папка: `/root/maqas`.
- Compose-проект: `maqas`.
- Существующий том PostgreSQL: `maqas_postgres_data`.
- Сертификат: AlphaSSL, оба имени `maqas.ru` и `www.maqas.ru`; действует до 31 марта 2027, 21:54 UTC.
- Сертификат и ключ: `/root/maqas/deploy/certs/fullchain.pem` и `privkey.pem`. Ключ принадлежит root, права `600`.

Сборка фронтенда выполнена локально, готовый `dist` передан по SSH и упакован через `deploy/frontend.Dockerfile`. Это позволяет обновлять сайт на сервере с 1 ГБ памяти без сборки Node.js на сервере. Backend собран на сервере. Рабочие `.env` сохранены; миграции схемы базы не запускались.

После переключения проверены TLS с доверенной цепочкой, перенаправления, главная страница, JS/CSS, прямые ссылки SPA и API. Все четыре контейнера получили статус `healthy`.

## Резервная копия перед переключением

На сервере: `/root/maqas-deploy-backups/20260914T071646Z`.

- `database.dump` — архив PostgreSQL; проверен через `pg_restore --list`.
- `files/` — прежние конфиги и рабочие `.env`.
- `compose.rollback.json` — ссылки на сохранённые образы контейнеров до обновления.

Старый образ backend ранее был удалён при работающем контейнере, поэтому для отката сохранён снимок его файловой системы. Резервная папка доступна только root и не входит в Git. Не удаляй эти образы и резервные копии до завершения проверки приложения.

## Проверка состояния

На сервере:

```bash
cd /root/maqas
docker compose ps
bash deploy/check.sh
```

Инструкции обновления и замены сертификата: [README.md](README.md). После смены IP-адреса сайта на домен нужно заново войти в аккаунт: браузер хранит данные авторизации отдельно для каждого адреса.
